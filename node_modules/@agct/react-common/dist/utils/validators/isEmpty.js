(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define(['exports', './isNone'], factory);
  } else if (typeof exports !== "undefined") {
    factory(exports, require('./isNone'));
  } else {
    var mod = {
      exports: {}
    };
    factory(mod.exports, global.isNone);
    global.isEmpty = mod.exports;
  }
})(this, function (exports, _isNone) {
  'use strict';

  Object.defineProperty(exports, "__esModule", {
    value: true
  });

  var _isNone2 = _interopRequireDefault(_isNone);

  function _interopRequireDefault(obj) {
    return obj && obj.__esModule ? obj : {
      default: obj
    };
  }

  var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) {
    return typeof obj;
  } : function (obj) {
    return obj && typeof Symbol === "function" && obj.constructor === Symbol ? "symbol" : typeof obj;
  };

  /**
    isEmpty();                // true
    isEmpty(null);            // true
    isEmpty(undefined);       // true
    isEmpty('');              // true
    isEmpty([]);              // true
    isEmpty({});              // false
    isEmpty('Adam Hawkins');  // false
    isEmpty([0,1,2]);         // false
    isEmpty('\n\t');          // false
    isEmpty('  ');            // false
  */

  var isEmpty = function isEmpty(obj) {
    var none = (0, _isNone2.default)(obj);
    if (none) {
      return none;
    }

    if (typeof obj.size === 'number') {
      return !obj.size;
    }

    var objectType = typeof obj === 'undefined' ? 'undefined' : _typeof(obj);

    if (objectType === 'object') {
      var size = obj.size;
      if (typeof size === 'number') {
        return !size;
      }
    }

    if (typeof obj.length === 'number' && objectType !== 'function') {
      return !obj.length;
    }

    if (objectType === 'object') {
      var length = obj.length;
      if (typeof length === 'number') {
        return !length;
      }
    }

    return false;
  };

  exports.default = isEmpty;
});