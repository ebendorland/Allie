(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define(['exports', './isEmpty'], factory);
  } else if (typeof exports !== "undefined") {
    factory(exports, require('./isEmpty'));
  } else {
    var mod = {
      exports: {}
    };
    factory(mod.exports, global.isEmpty);
    global.isBlank = mod.exports;
  }
})(this, function (exports, _isEmpty) {
  'use strict';

  Object.defineProperty(exports, "__esModule", {
    value: true
  });

  var _isEmpty2 = _interopRequireDefault(_isEmpty);

  function _interopRequireDefault(obj) {
    return obj && obj.__esModule ? obj : {
      default: obj
    };
  }

  /**
    isBlank();                // true
    isBlank(null);            // true
    isBlank(undefined);       // true
    isBlank('');              // true
    isBlank([]);              // true
    isBlank('\n\t');          // true
    isBlank('  ');            // true
    isBlank({});              // false
    isBlank('\n\t Hello');    // false
    isBlank('Hello world');   // false
    isBlank([1,2,3]);         // false
  */

  var isBlank = function isBlank(obj) {
    return (0, _isEmpty2.default)(obj) || typeof obj === 'string' && obj.match(/\S/) === null;
  };

  exports.default = isBlank;
});