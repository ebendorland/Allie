(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define(['chai', './url'], factory);
  } else if (typeof exports !== "undefined") {
    factory(require('chai'), require('./url'));
  } else {
    var mod = {
      exports: {}
    };
    factory(global.chai, global.url);
    global.urlTest = mod.exports;
  }
})(this, function (_chai, _url) {
  'use strict';

  describe('createPath', function () {
    it('replaces all instances of the given parameters', function () {
      var result = (0, _url.createPath)('example/:id/new/:id', { id: 'test' });
      (0, _chai.expect)(result).to.equal('example/test/new/test');
    });

    it('replaces multiple parameters', function () {
      var result = (0, _url.createPath)('example/:one/new/:two', { one: 'ONE', two: 'TWO' });
      (0, _chai.expect)(result).to.equal('example/ONE/new/TWO');
    });
  });
});