(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define(['exports', 'react'], factory);
  } else if (typeof exports !== "undefined") {
    factory(exports, require('react'));
  } else {
    var mod = {
      exports: {}
    };
    factory(mod.exports, global.react);
    global.HighlightedText = mod.exports;
  }
})(this, function (exports, _react) {
  'use strict';

  Object.defineProperty(exports, "__esModule", {
    value: true
  });

  var _react2 = _interopRequireDefault(_react);

  function _interopRequireDefault(obj) {
    return obj && obj.__esModule ? obj : {
      default: obj
    };
  }

  var HighlightedText = function HighlightedText(_ref) {
    var searchQuery = _ref.searchQuery;
    var textCompare = _ref.textCompare;

    var searchQueryReg = new RegExp('(' + searchQuery + ')', 'gi');
    var parts = textCompare.split(searchQueryReg);
    return _react2.default.createElement(
      'span',
      { className: 'highlighted-text' },
      parts.map(function (part, index) {
        return part.toLowerCase() === searchQuery.toLowerCase() ? _react2.default.createElement(
          'span',
          { className: 'highlighted-text__highlight', key: index },
          part
        ) : _react2.default.createElement(
          'span',
          { key: index },
          part
        );
      })
    );
  };

  HighlightedText.propTypes = {
    searchQuery: _react.PropTypes.string.isRequired,
    textCompare: _react.PropTypes.string.isRequired
  };

  exports.default = HighlightedText;
});