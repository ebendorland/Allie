/**
  isNone();              // true
  isNone(null);          // true
  isNone(undefined);     // true
  isNone('');            // false
  isNone([]);            // false
  isNone(function() {}); // false
*/

const isNone = obj => (
  obj === null || obj === undefined
);

export default isNone;
