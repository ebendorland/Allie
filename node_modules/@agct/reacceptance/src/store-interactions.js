import { createPath } from './url';
import { push } from 'react-router-redux';

export function dispatch(store, action) {
  if (!store) { throw new Error('Test Store Not Initialised.'); }
  store.dispatch(action);
}

export function visit(store, path, params = {}) {
  dispatch(store, push(createPath(path, params)));
}
